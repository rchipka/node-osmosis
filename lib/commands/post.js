/**
 * Make an HTTP POST request
 * @function post
 * @param {(string|contextCallback)} url - An absolute or relative URL or a
 * contextCallback that calls a URL.
 * @param {object} [data] - HTTP POST data
 * @memberof Command
 * @see Command.get
 */

module.exports.post = require('./get.js');
