/**
 * Set variables to use
 *
 * @param {object} object - { key: value } pairs of variables
 * @see Command.use
 */

var Using = function (context, data) {
}

module.exports.using = function (obj) {
    return Using;
}
