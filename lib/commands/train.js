/**
 * Teach Osmosis how to find content.
 *
 * @param {object} object - { name: selector } pairs of training variables
 * @see Command.learn
 */

var Train = function (context, data) {
}

module.exports.train = function (obj) {
    return Train;
}
